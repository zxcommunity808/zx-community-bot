'use strict';

const LAST_DAY_TOKEN = 'L';
function lastDayOfMonth(year, month) {
    return new Date(Date.UTC(year, month, 0)).getUTCDate();
}
function matchesDayOfMonth(field, year, month, day) {
    if (field.includes(day))
        return true;
    if (field.includes(LAST_DAY_TOKEN) && day === lastDayOfMonth(year, month))
        return true;
    return false;
}

exports.LAST_DAY_TOKEN = LAST_DAY_TOKEN;
exports.lastDayOfMonth = lastDayOfMonth;
exports.matchesDayOfMonth = matchesDayOfMonth;
//# sourceMappingURL=day-of-month.cjs.map
